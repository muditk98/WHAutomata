html
    body
        h1  #{product.id} - #{product.name}
        hr
        h3 Product Name :  #{product.name}
        h3 Manufactured on: #{product.batch}
        h3 Expiry:  #{product.expiry}
        //- ul
        //-     each val in stackmapdetails
        //-         li
        //-             h3  Stack id : #{val.stack.id}
        //-             h3  Location of Stack : ( #{val.stack.x} + #{val.stack.y} )
        //-             h3  Count of #{product.name} in this stack : #{val.stack.count}
                    

        //- h3 :   #{stack.id}
        //- h3 count of products: #{stack.count}
        div
            h2 Do you want to delete this product?
            form(action="/deleteProduct" method="POST")
                input(type="text" name="id" placeholder="Productid" value= product.id readonly)
                br
                select(name="delete")
                    option(value="No", selected="true") No
                    option(value="Yes") Yes
                
                input(type="Submit" value="Submit")
            
        