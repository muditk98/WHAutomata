html
    body
	//- stackmapdetails is a list of stackproductmaps that contain this product
	//- stackmapdetails[0] gives a stackproductmap
	//- stackmapdetails[0].stack.id gives id of stack
	//- stackmapdetails[0].stack.x gives x location. similarly y
	//- stackmapdetails[0].count gives the count of this product in this stack
        h1  #{product.id} - #{product.name}
        hr
        h3 Product Name :  #{product.name}
        h3 Manufactured on: #{product.batch}
        h3 Expiry:  #{product.expiry}
        h3 Stack:   #{stack.id}
        h3 count of products: #{stack.count}
        div
            h2 Do you want to delete this page?
            form(action="/deleteProduct" method="POST")
                input(type="text" name="id" placeholder="Productid")
                br
                select(name="delete")
                    option(value="No", selected="true") No
                    option(value="Yes") Yes
                
                input(type="Submit" value="Submit")
            
        